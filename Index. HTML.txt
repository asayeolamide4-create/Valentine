<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>My Valentine ❤️</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(to bottom, #fff1f4, #ffe0e9);
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Georgia', serif;
      color: #6d0f24;
      text-align: center;
      overflow: hidden;
    }

    .card {
      background: white;
      padding: 45px 30px;
      border-radius: 25px;
      max-width: 420px;
      width: 90%;
      box-shadow: 0 15px 40px rgba(0,0,0,0.18);
      animation: fadeIn 2s ease;
    }

    h1 {
      font-size: 26px;
      margin-bottom: 15px;
    }

    p {
      font-size: 16px;
      line-height: 1.8;
      margin-bottom: 25px;
    }

    .question {
      font-size: 28px;
      margin: 25px 0;
      color: #c9184a;
    }

    button {
      padding: 16px 40px;
      font-size: 18px;
      border: none;
      border-radius: 35px;
      cursor: pointer;
      background-color: #ff4d6d;
      color: white;
      transition: transform 0.2s ease, background 0.2s ease;
    }

    button:hover {
      background-color: #e63950;
      transform: scale(1.05);
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(25px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .hearts {
      position: fixed;
      inset: 0;
      pointer-events: none;
    }

    .heart {
      position: absolute;
      color: #ff4d6d;
      font-size: 18px;
      animation: floatUp 7s linear infinite;
      opacity: 0.8;
    }

    @keyframes floatUp {
      from {
        transform: translateY(110vh);
        opacity: 1;
      }
      to {
        transform: translateY(-15vh);
        opacity: 0;
      }
    }
  </style>
</head>
<body>

  <div class="hearts" id="hearts"></div>

  <div class="card">
    <h1>My Love ❤️</h1>

    <p>
      I don’t know when it happened exactly,  
      but somewhere between our conversations,  
      your laughter, and the way you exist so effortlessly…  
      you became home to me.
    </p>

    <p>
      You make ordinary days feel special.  
      You make silence feel safe.  
      You make my heart feel understood in ways  
      I didn’t know it needed.
    </p>

    <p>
      Loving you isn’t loud or forced—  
      it’s gentle, certain, and real.  
      And if I had to choose every day again,  
      I’d still choose you.
    </p>

    <div class="question">
      Will you be my Valentine? 💕
    </div>

    <button onclick="sayYes()">Yes 💖</button>
  </div>

  <script>
    function sayYes() {
      alert("Thank you for choosing me. I promise to always choose you too ❤️");
    }

    const heartsContainer = document.getElementById("hearts");
    setInterval(() => {
      const heart = document.createElement("div");
      heart.className = "heart";
      heart.innerHTML = "❤️";
      heart.style.left = Math.random() * 100 + "vw";
      heart.style.fontSize = (Math.random() * 20 + 10) + "px";
      heartsContainer.appendChild(heart);

      setTimeout(() => heart.remove(), 7000);
    }, 600);
  </script>

</body>
</html>